return {
    -- blink.cmp via NvChad's bundled spec. Replaces nvim-cmp + 5 cmp-* plugins.
    -- Default keymaps match nvim-cmp, so existing muscle memory carries over.
    { import = "nvchad.blink.lazyspec" },
}
